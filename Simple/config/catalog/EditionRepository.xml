<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!DOCTYPE gsa-template
	PUBLIC "-//Art Technology Group, Inc.//DTD Dynamo Security//EN"
	"http://www.atg.com/dtds/gsa/gsa_1.0.dtd">
<gsa-template>
	<header>
		<name>Edition</name>
		<author>Julia Malyshko</author>
	</header>
	<item-descriptor name="edition" default="true">
		<!-- You can identify a repository’s default item descriptor, by setting 
			its <item-descriptor> default attribute to true. Each repository can have 
			a single default item descriptor. If a repository has only one item descriptor 
			definition, it is the default. If no default item descriptor is explicitly 
			identified, the first item descriptor in the XML file is the default. When 
			you use Repository methods such as getItem, createItem without specifying 
			an item descriptor, you use the default item descriptor. These methods are 
			not recommended for most applications, unless there is only one item type 
			in the repository. -->
		<table name="edition" type="primary" id-column-names="book_id, seq_num">
			<property name="id" column-name="book_id" data-type="String" />
			<property name="seq_num" column-name="seq_num" data-type="String" />
			<property name="editionName" column-name="edition_name" data-type="String" required="true" />
		</table>
	</item-descriptor>
</gsa-template>

